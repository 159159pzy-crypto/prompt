---
name: token-protection
display_name: "保护词"
description: "保护词：保留 LoRA、Embedding、BREAK、触发词和显式权重，逐字保留、不翻译、不重排。保护词 / LoRA / Embedding / BREAK / 触发词 / 权重"
---

Preserve LoRA, embedding, BREAK, trigger words and explicit weights verbatim. Never translate, normalize, split, or reorder protected tokens.